@inherits LayoutComponentBase
@inject IJSRuntime JsRuntime

@if (isLoading)
{
    @* <div id="page-loader">
        <div class="container-mid">
            <img src="themes/images/loading-logo.png" class="img-responsive" alt="image">
            <div class="spinner-container">
                <div class="css-spinner"></div>
            </div>
        </div>
    </div> *@
}
@* <div id="page-loader">
    <div class="container-mid">
        <img src="themes/images/loading-logo.png" class="img-responsive" alt="image">
        <div class="spinner-container">
            <div class="css-spinner"></div>
        </div>
    </div>
</div> *@
@Body

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

@code {
    private bool isLoading = true;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // await Task.Delay(TimeSpan.FromSeconds(5));
            // await JsRuntime.InvokeVoidAsync("setLoader", true);

            // await Task.Delay(TimeSpan.FromSeconds(2));
            // await JsRuntime.InvokeVoidAsync("setLoader", false);

            // isLoading = false;
            // StateHasChanged();
        }
    }
}